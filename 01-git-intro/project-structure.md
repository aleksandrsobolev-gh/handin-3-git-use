## Project structure

- Keep source code in clear folders (e.g. `src/`, `docs/`)
- Use meaningful commit messages (feat/fix/docs/chore)
- Work in feature branches and merge via Pull Requests
- Keep binaries (pdf/zip/psd) out of normal Git history using Git LFS